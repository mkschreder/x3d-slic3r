Slic3r + GPX
------------

Usage: 
	./slice <model path>.stl 

Note: slice is only an example script. Modify it to your preferences. Input
path is expected NOT to have an extension. ".stl" extension is assumed for
input file. Output files (gcode + x3d) are created in the same directory as the
input file. 

Outputs x3d file.
